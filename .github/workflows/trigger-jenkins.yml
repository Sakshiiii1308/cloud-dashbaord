name: Trigger Jenkins
on:
  push:
    branches:
      - main

jobs:
  trigger-jenkins:
    runs-on: self-hosted

    steps:
      - name: Trigger Jenkins
        run: |
          curl -i -X POST \
          "http://192.168.111.113:8080/job/cloud-dashboard/build?token=github-trigger"
